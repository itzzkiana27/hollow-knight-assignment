diff --git a/core/src/main/java/com/hollowknight/audio/GameSfxPlayer.java b/core/src/main/java/com/hollowknight/audio/GameSfxPlayer.java
index 78597ef..6e19589 100644
--- a/core/src/main/java/com/hollowknight/audio/GameSfxPlayer.java
+++ b/core/src/main/java/com/hollowknight/audio/GameSfxPlayer.java
@@ -24,6 +24,9 @@ public final class GameSfxPlayer {
     private final Sound[] soulGainSounds;
 
     private final Sound enemyDamageSound;
+    private final Sound breakableWallHit1Sound;
+    private final Sound breakableWallHit2Sound;
+    private final Sound breakableWallDestroyedSound;
     private final Sound focusReadySound;
     private final Sound focusChargingSound;
     private final Sound focusHealSound;
@@ -57,6 +60,15 @@ public final class GameSfxPlayer {
         );
 
         enemyDamageSound = load("sword_hit_reject.wav");
+        breakableWallHit1Sound = load(
+            "breakable_wall/breakable_wall_hit_1.wav"
+        );
+        breakableWallHit2Sound = load(
+            "breakable_wall/breakable_wall_hit_2.wav"
+        );
+        breakableWallDestroyedSound = load(
+            "breakable_wall/breakable_wall_destroyed.wav"
+        );
         focusReadySound = load("focus_ready.wav");
         focusChargingSound = load("focus_health_charging.wav");
         focusHealSound = load("focus_health_heal.wav");
@@ -74,6 +86,18 @@ public final class GameSfxPlayer {
         play(enemyDamageSound, 0.42f);
     }
 
+    public void playBreakableWallHit1() {
+        play(breakableWallHit1Sound, 0.72f);
+    }
+
+    public void playBreakableWallHit2() {
+        play(breakableWallHit2Sound, 0.78f);
+    }
+
+    public void playBreakableWallDestroyed() {
+        play(breakableWallDestroyedSound, 0.86f);
+    }
+
     public void playSoulGain() {
         playRandom(soulGainSounds, 0.55f);
     }
@@ -122,6 +146,9 @@ public final class GameSfxPlayer {
         disposeAll(soulGainSounds);
 
         dispose(enemyDamageSound);
+        dispose(breakableWallHit1Sound);
+        dispose(breakableWallHit2Sound);
+        dispose(breakableWallDestroyedSound);
         dispose(focusReadySound);
         dispose(focusChargingSound);
         dispose(focusHealSound);
diff --git a/core/src/main/java/com/hollowknight/controller/GameController.java b/core/src/main/java/com/hollowknight/controller/GameController.java
index 9b5add8..bcc5f43 100644
--- a/core/src/main/java/com/hollowknight/controller/GameController.java
+++ b/core/src/main/java/com/hollowknight/controller/GameController.java
@@ -4040,6 +4040,8 @@ public class GameController {
             result
                 == CrackedWall.HitResult.DESTROYED
         ) {
+            gameSfxPlayer.playBreakableWallDestroyed();
+
             if (crackedWallPlatform != null) {
                 platformWorld.removePlatform(
                     crackedWallPlatform
@@ -4047,6 +4049,13 @@ public class GameController {
             }
 
             unlockVoidHeartReward();
+        } else if (
+            result
+                == CrackedWall.HitResult.BROKEN_APPEARANCE
+        ) {
+            gameSfxPlayer.playBreakableWallHit2();
+        } else {
+            gameSfxPlayer.playBreakableWallHit1();
         }
 
         return true;
diff --git a/core/src/main/java/com/hollowknight/view/screens/GameScreen.java b/core/src/main/java/com/hollowknight/view/screens/GameScreen.java
index faae66e..3acf3c2 100644
--- a/core/src/main/java/com/hollowknight/view/screens/GameScreen.java
+++ b/core/src/main/java/com/hollowknight/view/screens/GameScreen.java
@@ -1083,7 +1083,14 @@ public class GameScreen extends ScreenAdapter {
             ShapeRenderer.ShapeType.Filled
         );
 
-        shapeRenderer.setColor(Color.BLACK);
+        // Blend the hidden-room cover into the Crossroads
+        // background instead of showing a pure-black rectangle.
+        shapeRenderer.setColor(
+            0.020f,
+            0.024f,
+            0.052f,
+            1f
+        );
 
         shapeRenderer.rect(
             hiddenRoom.x,
